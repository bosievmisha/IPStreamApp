Для работы на linux(Debian/Ubuntu)
    
    sudo apt-get update
    sudo apt-get install portaudio19-dev libasound2-dev
    sudo apt install ffmpeg
Для работы на Windows также нужен ffmpeg
https://ffmpeg.org/download.html
